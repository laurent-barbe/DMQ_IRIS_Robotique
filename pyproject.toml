[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "DMQ IRIS Robotique"
authors = [{name = "Barbé Laurent", email = "laurent.barbe@unistra.fr"}]
dynamic = ["version", "description"]
